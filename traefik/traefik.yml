entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

certificatesResolvers:
  myresolver:
    acme:
      email: your@email.com
      storage: /letsencrypt/acme.json
      tlsChallenge: true

providers:
  docker:
    exposedByDefault: false
